// Signing for builds that are distributed outside the App Store (make build / make archive).
// Requires a "Developer ID Application" certificate for this team in the login keychain.
DEVELOPMENT_TEAM = 72BLQYHMTN
DEVELOPMENT_TEAM[sdk=macosx*] = 72BLQYHMTN
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = Developer ID Application
CODE_SIGN_IDENTITY[sdk=macosx*] = Developer ID Application
PROVISIONING_PROFILE_SPECIFIER =
PROVISIONING_PROFILE_SPECIFIER[sdk=macosx*] =
